server:
  port: 9797
  
spring:
  datasource:
    url: jdbc:sqlserver://localhost:56219;DatabaseName=user_db;trustServerCertificate=true
    username: sa
    password: admin123$
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
   
   
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect  